#if !defined(HEXADECIMAL_H)
#define HEXADECIMAL_H

#include <string>
#include <cctype>
#include <vector>
#include <cmath>
#include <iostream>

using std::string;
using std::vector;

namespace hexadecimal 
{
    int convert(string hex);
}  // namespace hexadecimal

#endif // HEXADECIMAL_H